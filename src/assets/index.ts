// Asset exports for organized imports
// This file serves as the main entry point for all assets in the src/assets directory

// Image assets
export const Images = {
  // Add image imports here as needed
  // Example: logo: new URL('./images/logo.png', import.meta.url).href,
};

// Video assets
export const Videos = {
  // Add video imports here as needed
  // Example: heroVideo: new URL('./videos/hero.mp4', import.meta.url).href,
};

// Icon assets
export const Icons = {
  // Add icon imports here as needed
  // Example: closeIcon: new URL('./icons/close.svg', import.meta.url).href,
};

// Font assets
export const Fonts = {
  // Add font imports here as needed
  // Example: primaryFont: new URL('./fonts/primary.woff2', import.meta.url).href,
};

// Style assets
export const Styles = {
  // Add style imports here as needed
  // Example: variables: new URL('./styles/variables.css', import.meta.url).href,
};

// Default export for convenience
export default {
  Images,
  Videos,
  Icons,
  Fonts,
  Styles,
};
